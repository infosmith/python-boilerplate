[project]
name = "{{ project_name }}"
version = "0.0.0"
description = "{{ project_description }}"
readme = "README.md"
requires-python = ">={{ project_python_version }}"


[tool.bandit]
exclude_dirs = ["tests", "tests"]
